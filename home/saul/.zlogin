# Use this to manage the startup in absence of a login manager (using LightDM now)

# I commented since I can just start the console with startx
# if [ -z "${DISPLAY}" ] && [ "${XDG_VTNR}" -eq 1 ]; then
#   startx
# fi
