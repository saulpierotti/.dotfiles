#!/bin/sh
# this file is not used at the moment since LighDM uses .xprofile and launches the wm autonomously
# numlockx &
# source /etc/X11/xinit/xinitrc.d/50-systemd-user.sh
# export QT_QPA_PLATFORMTHEME=qt5ct
# exec i3
