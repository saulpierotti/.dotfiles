#!/bin/sh
# this is required by LighDM to load the env with zsh
[[ -f ~/.zshenv ]] && source ~/.zshenv

# For LightDM, copied from .xinitrc
# source /etc/X11/xinit/xinitrc.d/50-systemd-user.sh # do not remember the reason
export QT_QPA_PLATFORMTHEME=qt5ct # dark theme in qt apps
